# Core dependencies - ordered by installation priority
# Install numpy first (foundation for all scientific packages)
numpy>=1.24.0,<2.0.0

# Core ML/Data processing
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Vision libraries
Pillow>=10.0.0
opencv-python>=4.8.0

# MediaPipe (note: requires Python 3.8-3.12 currently)
# If you're on Python 3.13+, use Python 3.12 instead:
# pyenv install 3.12.0 && pyenv local 3.12.0
mediapipe>=0.10.0

# Visualization
matplotlib>=3.7.0

# YouTube video downloading (for WLASL pipeline)
yt-dlp>=2025.01.0

# Real-time Communication
python-socketio>=5.9.0
python-engineio>=4.7.0
